(()=>{
  const ADMIN='automly1@gmail.com';
  const SHEET_ID='1mFDJKBexMfMn8NZSq7xhES7pHWt4LCEY2Gq-zATHuco';
  const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
  const clean=v=>String(v==null?'':v).trim();
  const low=v=>clean(v).toLowerCase();
  const num=v=>{v=Number(v||0);return Number.isFinite(v)?Math.round(v):0};
  const esc=v=>clean(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
  const parse=(r,f)=>{try{return r?JSON.parse(r):f}catch{return f}};
  const planRules={free:{label:'Free',searches:1,credits:0,depth:10,csv:false},starter:{label:'Starter',searches:5,credits:50,depth:25,csv:true},growth:{label:'Growth',searches:15,credits:250,depth:50,csv:true},launch:{label:'Launch Plan',searches:'custom',credits:'custom',depth:'custom',csv:true},admin:{label:'Admin Unlimited',searches:'Unlimited',credits:'Unlimited',depth:'Unlimited',csv:true}};
  let data={users:[],searches:[],final_leads:[],seo_audits:[],raw_prospects:[],contacts:[],lead_feedback:[]};
  let users=[],leads=[],diagRows=[],feedbackRows=[],integrityIssues=[];

  function session(){
    if(window.rankforgeAuth?.getSession){let s=window.rankforgeAuth.getSession();if(s)return s}
    try{for(let i=0;i<localStorage.length;i++){let k=localStorage.key(i)||'';if(!/^sb-.+-auth-token$/.test(k))continue;let p=parse(localStorage.getItem(k),{}),u=p.user||p.currentSession?.user||p.session?.user;if(u?.id)return{userId:u.id,email:u.email||''}}}catch{}
    return parse(localStorage.getItem('rankforge-auth-session-v1'),{})||{};
  }
  function email(){let s=session();return low(s.email||s.userEmail)}
  function admin(){return email()===ADMIN}
  function show(id){let n=$(id);if(n)n.hidden=false} function hide(id){let n=$(id);if(n)n.hidden=true}
  function text(id,v){let n=document.getElementById(id);if(n)n.textContent=clean(v)}
  function status(v,t){let n=$('#rfAdminStatus');if(!n)return;n.textContent=v;n.className='rf-pill '+(t==='error'?'rf-badge-error':t==='ok'?'rf-badge-ok':'')}
  function fmtDate(v){let d=new Date(v||'');return Number.isNaN(d.getTime())?'—':new Intl.DateTimeFormat('en-US',{month:'short',day:'numeric',year:'numeric'}).format(d)}
  function idUser(r){return clean(r.user_id||r.owner_user_id||r.userId||r.id||r.email||r.user_email)}
  function idLead(r){return clean(r.lead_id||r.id||r.audit_id||r.prospect_id)}
  function idSearch(r){return clean(r.search_id||r.list_id||r.search_batch_id||r.batch_id||r.saved_list_id||r.listId)}
  function company(r){return clean(r.company_name||r.business_name||r.name||r.company||'Unknown company')}
  function planKey(v){v=low(v||'free');if(/admin|unlimited/.test(v))return'admin';if(/growth/.test(v))return'growth';if(/starter/.test(v))return'starter';if(/launch|founding|custom|legacy/.test(v))return'launch';return'free'}
  function bill(v){v=low(v||'unknown');if(/active|paid|admin_unlimited/.test(v))return'active';if(/pending|incomplete/.test(v))return'pending';if(/past/.test(v))return'past_due';if(/cancel/.test(v))return'canceled';if(/free/.test(v))return'free';return v||'unknown'}
  function verified(r){let v=low(r.seo_claims_verified||r.verified_seo_evidence||r.evidence_status);return ['true','yes','verified'].includes(v)}
  function partial(r){let v=low(r.seo_claims_verified||r.verified_seo_evidence||r.evidence_status);return v==='partial'}
  function signals(r){return num(r.seo_evidence_signal_count||r.evidence_signal_count||r.signal_count)}
  function credit(r){return low(r.lead_credit_counted)==='true'||r.lead_credit_counted===true}
  function qstatus(r){return low(r.qualification_status||r.status||'review_needed')}
  function contact(r){return !!(clean(r.email||r.decision_maker_email||r.phone||r.decision_maker_phone)||low(r.phone_visible)==='true'||low(r.email_visible)==='true'||low(r.contact_page_found)==='true'||low(r.contact_cta_found)==='true')}
  function htmlBytes(r){return num(r.homepage_html_bytes_analyzed||r.html_bytes||r.homepage_html_bytes)}
  function textWords(r){return num(r.homepage_text_words_analyzed||r.text_words||r.homepage_text_words)}
  function crawlSource(r){return clean(r.crawl_source_field||r.crawl_source||r.seo_score_source||'none')}

  function parseGvizTable(parsed){let cols=(parsed.table?.cols||[]).map(c=>c.label||c.id);return (parsed.table?.rows||[]).map(row=>{let o={};(row.c||[]).forEach((cell,i)=>o[cols[i]]=cell?clean(cell.f||cell.v||''):'');return o})}
  function fetchSheet(sheet){return new Promise(resolve=>{let cb='rfAdminQuality_'+sheet+'_'+Date.now()+'_'+Math.floor(Math.random()*9999),script=document.createElement('script'),done=false;let timer=setTimeout(()=>finish([]),15000);function finish(rows){if(done)return;done=true;clearTimeout(timer);script.remove();try{delete window[cb]}catch{window[cb]=undefined}resolve(rows)}window[cb]=p=>{try{finish(parseGvizTable(p))}catch{finish([])}};script.src='https://docs.google.com/spreadsheets/d/'+encodeURIComponent(SHEET_ID)+'/gviz/tq?sheet='+encodeURIComponent(sheet)+'&tqx=responseHandler:'+encodeURIComponent(cb);script.async=true;script.onerror=()=>finish([]);document.body.appendChild(script)})}

  function accessGate(){
    hide('#rfAdminContent'); hide('#rfAccessDenied'); hide('#rfAdminLoading');
    if(!email()){show('#rfAdminSignin');return false}
    if(!admin()){show('#rfAccessDenied');return false}
    show('#rfAdminLoading');return true;
  }

  function buildUsers(){
    let map={};
    [...data.users,...data.searches,...data.final_leads,...data.lead_feedback].forEach(r=>{let id=idUser(r);if(!id)return;if(!map[id])map[id]={user_id:id,email:clean(r.email||r.user_email),plan:clean(r.plan||r.current_plan||r.subscription_plan||r.package||r.tier||'Free'),billing_status:clean(r.billing_status||r.subscription_status||r.status||'unknown'),searches:0,credits:0,review:0,leads:0,feedback:0,last_active_at:clean(r.last_active_at||r.updated_at||r.created_at),notes:clean(r.notes||r.admin_note||''),override_json:clean(r.override_json||'')};if(!map[id].email&&clean(r.email||r.user_email))map[id].email=clean(r.email||r.user_email);if(map[id].plan==='Free'&&clean(r.plan||r.current_plan||r.subscription_plan))map[id].plan=clean(r.plan||r.current_plan||r.subscription_plan);if(clean(r.billing_status||r.subscription_status))map[id].billing_status=clean(r.billing_status||r.subscription_status);let d=clean(r.last_active_at||r.updated_at||r.created_at);if(d&&(!map[id].last_active_at||new Date(d)>new Date(map[id].last_active_at)))map[id].last_active_at=d});
    data.searches.forEach(r=>{let u=map[idUser(r)];if(u)u.searches++});
    data.final_leads.forEach(r=>{let u=map[idUser(r)];if(!u)return;u.leads++;if(qstatus(r)==='qualified'&&credit(r))u.credits++;if(['review_needed','needs_review','review'].includes(qstatus(r)))u.review++});
    data.lead_feedback.forEach(r=>{let u=map[idUser(r)];if(u)u.feedback++});
    return Object.values(map).sort((a,b)=>(b.searches+b.leads+b.feedback)-(a.searches+a.leads+a.feedback));
  }

  function enrichLeads(){
    let auditByLead={};data.seo_audits.forEach(a=>{let k=idLead(a)||clean(a.prospect_id);if(k)auditByLead[k]=a});
    return data.final_leads.map((l,i)=>({...auditByLead[idLead(l)]||{},...l,_idx:i}));
  }

  function buildDiag(){return leads.map(l=>{let warns=[];if(low(crawlSource(l))==='none'||!crawlSource(l))warns.push('Missing crawl source');if(htmlBytes(l)===0)warns.push('No HTML analyzed');if(textWords(l)===0)warns.push('No text extracted');if(!verified(l)&&signals(l)===0)warns.push('No verified evidence');if(qstatus(l)==='qualified'&&!verified(l))warns.push('Gate issue: qualified without verified evidence');if(credit(l)&&qstatus(l)!=='qualified')warns.push('Credit issue: non-qualified counted');return{lead:l,warnings:warns}})}
  function buildIntegrity(){
    let issues=[];
    leads.forEach(l=>{let st=qstatus(l),v=verified(l),sig=signals(l),ct=contact(l),cr=credit(l);if(st==='qualified'&&!cr)issues.push([l,'Qualified lead not counted']);if(st==='review_needed'&&cr)issues.push([l,'Review Needed lead is credit counted']);if(st==='rejected'&&cr)issues.push([l,'Rejected lead is credit counted']);if(st==='qualified'&&(!v||sig<3))issues.push([l,'Qualified lead missing verified evidence']);if(st==='qualified'&&!ct)issues.push([l,'Qualified lead missing contact path'])});
    return issues;
  }

  function renderKpis(){let verifiedCount=leads.filter(verified).length,rate=leads.length?Math.round(verifiedCount/leads.length*100)+'%':'0%',creditBad=leads.some(l=>credit(l)&&qstatus(l)!=='qualified'),gateBad=leads.some(l=>qstatus(l)==='qualified'&&!verified(l));text('kpiUsers',users.length);text('kpiSearches',data.searches.length);text('kpiFinalLeads',leads.length);text('kpiFeedback',data.lead_feedback.length);text('kpiEvidenceRate',rate);text('kpiCreditIntegrity',(creditBad||gateBad)?'Needs Review':'OK')}
  function healthStatus(ok,warn,err){return err?'Error':warn?'Needs Review':'OK'}
  function healthClass(s){return s==='OK'||s==='Healthy'?'rf-status-ok':s==='Error'||s==='Critical'?'rf-status-error':'rf-status-warn'}
  function renderHealth(){let crawlAvail=leads.filter(l=>crawlSource(l)&&low(crawlSource(l))!=='none').length,empty=leads.filter(l=>htmlBytes(l)===0||textWords(l)===0).length,blocked=leads.filter(l=>/block|noindex|403|401|429|500/.test(low(l.http_status||l.indexability_status||l.crawl_debug))).length,avgHtml=leads.length?Math.round(leads.reduce((a,l)=>a+htmlBytes(l),0)/leads.length):0;let ver=leads.filter(verified).length,part=leads.filter(partial).length,noev=leads.filter(l=>!verified(l)&&!partial(l)).length,avgSig=leads.length?(leads.reduce((a,l)=>a+signals(l),0)/leads.length).toFixed(1):'0.0';let q=leads.filter(l=>qstatus(l)==='qualified').length,qNoEv=leads.filter(l=>qstatus(l)==='qualified'&&!verified(l)).length,qNoContact=leads.filter(l=>qstatus(l)==='qualified'&&!contact(l)).length,hiRev=leads.filter(l=>['review_needed','needs_review','review'].includes(qstatus(l))&&num(l.overall_lead_score||l.seo_need_score)>=70).length;let cq=leads.filter(l=>qstatus(l)==='qualified'&&credit(l)).length,cnq=leads.filter(l=>qstatus(l)!=='qualified'&&credit(l)).length,rr=leads.filter(l=>['review_needed','needs_review','review','rejected'].includes(qstatus(l))&&credit(l)).length;let cards=[['Monitor','Crawl Health','Check empty HTML, blocked URLs, and crawl source fields.',[['Crawl source available',crawlAvail],['Empty HTML/text rows',empty],['Blocked/noindex rows',blocked],['Avg. HTML bytes',avgHtml]],healthStatus(true,empty||blocked,empty>leads.length*.5)],['Verify','Evidence Health','Track no-evidence and partial-evidence rates.',[['Verified evidence',ver],['Partial evidence',part],['No verified evidence',noev],['Avg. evidence signals',avgSig]],healthStatus(true,noev>ver,noev>leads.length*.7&&leads.length)],['Protect','Gate Health','Qualified should require evidence plus contact path.',[['Qualified leads',q],['Qualified missing evidence',qNoEv],['Qualified missing contact',qNoContact],['Review high-score leads',hiRev]],healthStatus(true,qNoEv||qNoContact,false)],['Audit','Credit Integrity','Only qualified leads should count against credits.',[['Credit-counted qualified',cq],['Credit-counted non-qualified',cnq],['Review/rejected using credits',rr]],healthStatus(true,cnq||rr,false)]];$('#qualityHealthGrid').innerHTML=cards.map(c=>`<article class="rf-health-card"><p class="rf-eyebrow">${esc(c[0])}</p><h3>${esc(c[1])}</h3><p>${esc(c[2])}</p><div class="rf-health-metrics">${c[3].map(m=>`<div><span>${esc(m[0])}</span><strong>${esc(m[1])}</strong></div>`).join('')}</div><span class="rf-status ${healthClass(c[4])}">${esc(c[4])}</span></article>`).join('')}

  function renderDiagnosticCards(){let vals=[['Crawl Source Missing',diagRows.filter(r=>r.warnings.includes('Missing crawl source')).length],['HTML Bytes = 0',leads.filter(l=>htmlBytes(l)===0).length],['Text Words = 0',leads.filter(l=>textWords(l)===0).length],['HTTP blocked/errors',leads.filter(l=>/block|403|401|429|500/.test(low(l.http_status||l.crawl_debug))).length],['Claims verified true',leads.filter(verified).length],['Signals ≥ 3',leads.filter(l=>signals(l)>=3).length]];$('#diagnosticCards').innerHTML=vals.map(v=>`<article class="rf-mini-card"><span>${esc(v[0])}</span><strong>${esc(v[1])}</strong></article>`).join('')}
  function renderDiagnostics(){renderDiagnosticCards();let rows=diagRows.filter(r=>r.warnings.length).slice(0,150);let tb=$('#diagnosticTable tbody');tb.innerHTML=rows.length?rows.map(r=>{let l=r.lead;return `<tr><td><strong>${esc(company(l))}</strong><span class="rf-mono rf-user-id">${esc(idLead(l)||'—')}</span></td><td><span class="rf-mono">${esc(idUser(l)||'—')}</span></td><td><span class="rf-mono">${esc(idSearch(l)||'—')}</span></td><td>${esc(qstatus(l)||'—')}</td><td><span class="rf-mono">${esc(crawlSource(l)||'none')}</span></td><td><span class="rf-mono">${htmlBytes(l)}</span></td><td><span class="rf-mono">${textWords(l)}</span></td><td><span class="rf-mono">${esc(l.http_status||'—')}</span></td><td>${num(l.seo_issue_count)}</td><td>${signals(l)}</td><td>${verified(l)?'true':partial(l)?'partial':'false'}</td><td>${r.warnings.map(w=>`<span class="rf-badge rf-badge-warning">${esc(w)}</span>`).join(' ')}</td></tr>`}).join(''):'<tr><td colspan="12" class="rf-muted">No crawl or evidence warnings found.</td></tr>'}

  function renderUsers(){let q=low($('#userSearch')?.value),pf=$('#userPlanFilter')?.value||'all',bf=$('#userBillingFilter')?.value||'all';let rows=users.filter(u=>(!q||low(u.email+' '+u.user_id).includes(q))&&(pf==='all'||planKey(u.plan)===pf)&&(bf==='all'||bill(u.billing_status)===bf));let tb=$('#usersTable tbody');tb.innerHTML=rows.length?rows.map(u=>{let pk=planKey(u.plan),bs=bill(u.billing_status);return `<tr><td><strong>${esc(u.email||'No email recorded')}</strong><span class="rf-mono rf-user-id">${esc(u.user_id)}</span>${!u.email?'<span class="rf-badge rf-badge-warning">Identity missing</span>':''}</td><td>${esc(u.email||'No email recorded')}</td><td><span class="rf-badge rf-badge-plan">${esc(planRules[pk].label)}</span></td><td><span class="rf-badge ${bs==='active'?'rf-badge-active':bs==='pending'||bs==='past_due'?'rf-badge-warning':'rf-badge-free'}">${esc(bs)}</span></td><td>${u.searches}</td><td>${u.credits}</td><td>${u.review}</td><td>${u.leads}</td><td>${fmtDate(u.last_active_at)}</td><td><button class="button small ghost" data-view-user="${esc(u.user_id)}">View User</button> <button class="button small ghost" data-copy-user="${esc(u.user_id)}">Copy ID</button></td></tr>`}).join(''):'<tr><td colspan="10" class="rf-muted">No users loaded yet.</td></tr>'}

  function renderBilling(){let tb=$('#billingTable tbody');tb.innerHTML=users.length?users.map(u=>{let pk=planKey(u.plan),rule=planRules[pk],bs=bill(u.billing_status),searchLimit=rule.searches==='Unlimited'?'∞':rule.searches==='custom'?'custom':rule.searches,creditLimit=rule.credits==='Unlimited'?'∞':rule.credits==='custom'?'custom':rule.credits,remainingSearch=typeof rule.searches==='number'?Math.max(0,rule.searches-u.searches):searchLimit,remainingCredit=typeof rule.credits==='number'?Math.max(0,rule.credits-u.credits):creditLimit;return `<tr><td><strong>${esc(u.email||'No email recorded')}</strong><span class="rf-mono rf-user-id">${esc(u.user_id)}</span></td><td><select data-plan="${esc(u.user_id)}"><option value="free" ${pk==='free'?'selected':''}>Free</option><option value="starter" ${pk==='starter'?'selected':''}>Starter</option><option value="growth" ${pk==='growth'?'selected':''}>Growth</option><option value="launch" ${pk==='launch'?'selected':''}>Launch Plan</option><option value="admin" ${pk==='admin'?'selected':''}>Admin Unlimited</option></select></td><td><select data-billing="${esc(u.user_id)}"><option ${bs==='free'?'selected':''}>free</option><option ${bs==='active'?'selected':''}>active</option><option ${bs==='pending'?'selected':''}>pending</option><option ${bs==='canceled'?'selected':''}>canceled</option><option ${bs==='past_due'?'selected':''}>past_due</option><option ${bs==='admin_unlimited'?'selected':''}>admin_unlimited</option></select></td><td>${u.searches} / ${searchLimit}<br><span class="rf-muted">${remainingSearch} remaining</span></td><td>${u.credits} / ${creditLimit}<br><span class="rf-muted">${remainingCredit} remaining</span></td><td>${u.review}<br><span class="rf-muted">Does not use credits</span></td><td><input data-search-adjust="${esc(u.user_id)}" placeholder="Search +/-"><input data-lead-adjust="${esc(u.user_id)}" placeholder="Lead +/-"></td><td><input data-note="${esc(u.user_id)}" placeholder="Reason / note"></td><td><button class="button small primary" data-copy-override="${esc(u.user_id)}">Copy Override JSON</button><button class="button small ghost" data-clear-override="${esc(u.user_id)}">Clear</button></td></tr>`}).join(''):'<tr><td colspan="9" class="rf-muted">No users loaded yet.</td></tr>'}

  function ftype(r){let t=low(r.feedback_type||r.type||'other');if(/good/.test(t))return'good_lead';if(/contact/.test(t))return'bad_contact';if(/niche/.test(t))return'wrong_niche';if(/city|location/.test(t))return'wrong_city';if(/evidence/.test(t))return'no_evidence';return t||'other'}
  function renderFeedback(){feedbackRows=data.lead_feedback;let counts={good_lead:0,bad_contact:0,wrong_niche:0,wrong_city:0,no_evidence:0,other:0};feedbackRows.forEach(r=>{let t=ftype(r);counts[counts[t]!=null?t:'other']++});$('#feedbackCards').innerHTML=Object.entries(counts).map(([k,v])=>`<article class="rf-mini-card"><span>${esc(k.replace(/_/g,' '))}</span><strong>${v}</strong></article>`).join('');let type=$('#feedbackTypeFilter')?.value||'all',q=low($('#feedbackSearch')?.value);let rows=feedbackRows.filter(r=>(type==='all'||ftype(r)===type)&&(!q||low(Object.values(r).join(' ')).includes(q))).slice(0,100);let tb=$('#feedbackTable tbody');tb.innerHTML=rows.length?rows.map(r=>`<tr><td><span class="rf-badge ${ftype(r)==='good_lead'?'rf-badge-ok':'rf-badge-warning'}">${esc(ftype(r).replace(/_/g,' '))}</span></td><td>${esc(company(r))}</td><td><span class="rf-mono">${esc(idUser(r)||r.email||'—')}</span></td><td><span class="rf-mono">${esc(idSearch(r)||'—')}</span></td><td>${esc(r.note||r.feedback_note||'No note')}</td><td>${fmtDate(r.created_at)}</td><td><button class="button small ghost" data-copy-report="feedback:${esc(idLead(r)||idSearch(r))}">Copy</button></td></tr>`).join(''):'<tr><td colspan="7"><div class="rf-empty"><h2>No feedback submitted yet</h2><p>Feedback will appear after users review leads and submit quality signals.</p></div></td></tr>'}

  function renderIntegrity(){let checks=[['Qualified leads counted','qualification_status = qualified AND lead_credit_counted = true',leads.filter(l=>qstatus(l)==='qualified'&&!credit(l)).length],['Review leads not counted','review_needed AND lead_credit_counted != true',leads.filter(l=>['review_needed','needs_review','review'].includes(qstatus(l))&&credit(l)).length],['Rejected leads not counted','rejected AND lead_credit_counted != true',leads.filter(l=>qstatus(l)==='rejected'&&credit(l)).length],['Qualified evidence gate','qualified leads must have verified evidence and signal_count >= 3',leads.filter(l=>qstatus(l)==='qualified'&&(!verified(l)||signals(l)<3)).length],['Qualified contact gate','qualified leads must have phone/email/contact path',leads.filter(l=>qstatus(l)==='qualified'&&!contact(l)).length]];$('#integrityChecks').innerHTML=checks.map(c=>`<article class="rf-integrity-card"><span class="rf-status ${c[2]?'rf-status-warn':'rf-status-ok'}">${c[2]?'Warning':'OK'}</span><h3>${esc(c[0])}</h3><p>${esc(c[1])}</p><strong>${c[2]} affected</strong><br><button class="button small ghost" data-view-integrity="${esc(c[0])}">View rows</button></article>`).join('');let tb=$('#integrityTable tbody');tb.innerHTML=integrityIssues.length?integrityIssues.slice(0,150).map(([l,issue])=>`<tr><td>${esc(company(l))}<span class="rf-mono rf-user-id">${esc(idLead(l)||'—')}</span></td><td><span class="rf-mono">${esc(idUser(l)||'—')}</span></td><td>${esc(qstatus(l))}</td><td>${credit(l)?'true':'false'}</td><td>${verified(l)?'true':partial(l)?'partial':'false'}</td><td>${signals(l)}</td><td>${contact(l)?'yes':'no'}</td><td><span class="rf-badge rf-badge-warning">${esc(issue)}</span></td><td>Review gate/credit fields before syncing corrections.</td></tr>`).join(''):'<tr><td colspan="9" class="rf-muted">Credit integrity checks passed.</td></tr>'}

  function renderActivity(){let box=$('#adminActivity');let overrides=users.filter(u=>u.override_json||u.notes);box.innerHTML=overrides.length?overrides.slice(0,20).map(u=>`<article><strong>${esc(u.email||u.user_id)}</strong><p class="rf-muted">${esc(u.notes||'Override JSON present')}</p><span class="rf-mono">${esc(u.override_json||'')}</span></article>`).join(''):'<div class="rf-admin-note">No admin overrides saved yet. Overrides are currently managed through JSON copy/apply flow.</div>'}

  function renderAll(){users=buildUsers();leads=enrichLeads();diagRows=buildDiag();integrityIssues=buildIntegrity();renderKpis();renderHealth();renderDiagnostics();renderUsers();renderBilling();renderFeedback();renderIntegrity();renderActivity();hide('#rfAdminLoading');show('#rfAdminContent');status('Admin data synced','ok')}
  async function load(){if(!accessGate())return;status('Loading admin quality data…');let sheets=['users','searches','final_leads','seo_audits','raw_prospects','contacts','lead_feedback'];let pairs=await Promise.all(sheets.map(s=>fetchSheet(s).then(r=>[s,r])));pairs.forEach(([s,r])=>data[s]=r||[]);renderAll()}

  function overrideJSON(userId){let u=users.find(x=>x.user_id===userId)||{},p=document.querySelector(`[data-plan="${CSS.escape(userId)}"]`)?.value||planKey(u.plan),b=document.querySelector(`[data-billing="${CSS.escape(userId)}"]`)?.value||bill(u.billing_status),sa=document.querySelector(`[data-search-adjust="${CSS.escape(userId)}"]`)?.value||'',la=document.querySelector(`[data-lead-adjust="${CSS.escape(userId)}"]`)?.value||'',note=document.querySelector(`[data-note="${CSS.escape(userId)}"]`)?.value||'';return{user_id:userId,email:u.email||'',plan_override:p,billing_status_override:b,search_credit_adjustment:sa,lead_credit_adjustment:la,note,reason:note,updated_at:new Date().toISOString(),source:'rankforge_admin_quality'}}
  async function copyText(t){try{await navigator.clipboard.writeText(t);status('Copied to clipboard','ok')}catch{status('Copy failed','error')}}
  function exportDiagnostics(){let report={created_at:new Date().toISOString(),kpis:{users:users.length,searches:data.searches.length,final_leads:leads.length,feedback:data.lead_feedback.length},integrity_issues:integrityIssues.map(([l,i])=>({lead_id:idLead(l),user_id:idUser(l),company:company(l),issue:i})),diagnostics:diagRows.filter(r=>r.warnings.length).map(r=>({lead_id:idLead(r.lead),company:company(r.lead),warnings:r.warnings}))};let blob=new Blob([JSON.stringify(report,null,2)],{type:'application/json'}),a=document.createElement('a'),url=URL.createObjectURL(blob);a.href=url;a.download='rankforge-admin-diagnostics.json';a.click();URL.revokeObjectURL(url)}
  function userDrawer(userId){let u=users.find(x=>x.user_id===userId);if(!u)return;let userLeads=leads.filter(l=>idUser(l)===userId).slice(0,10),userSearches=data.searches.filter(s=>idUser(s)===userId).slice(0,10);$('#drawerBody').innerHTML=`<p><strong>${esc(u.email||'No email recorded')}</strong><span class="rf-mono rf-user-id">${esc(u.user_id)}</span></p><div class="rf-health-metrics"><div><span>Plan</span><strong>${esc(planRules[planKey(u.plan)].label)}</strong></div><div><span>Billing</span><strong>${esc(bill(u.billing_status))}</strong></div><div><span>Search usage</span><strong>${u.searches}</strong></div><div><span>Qualified credits</span><strong>${u.credits}</strong></div><div><span>Review Needed</span><strong>${u.review}</strong></div></div><h3>Last searches</h3><ul>${userSearches.map(s=>`<li>${esc(idSearch(s)||'—')} · ${esc(s.niche||s.target_service||'Batch')} · ${fmtDate(s.created_at)}</li>`).join('')||'<li>No searches found.</li>'}</ul><h3>Recent leads</h3><ul>${userLeads.map(l=>`<li>${esc(company(l))} · ${esc(qstatus(l))}</li>`).join('')||'<li>No leads found.</li>'}</ul><h3>Override JSON</h3><pre class="rf-json-box">${esc(JSON.stringify(overrideJSON(userId),null,2))}</pre>`;$('#adminDrawer').hidden=false}

  function bind(){
    $('#refreshAdminData')?.addEventListener('click',load);$('#exportDiagnostics')?.addEventListener('click',exportDiagnostics);$('#copyOverrides')?.addEventListener('click',()=>copyText(JSON.stringify(users.map(u=>overrideJSON(u.user_id)),null,2)));
    $$('.rf-admin-tabs a').forEach(a=>a.addEventListener('click',()=>{$$('.rf-admin-tabs a').forEach(x=>x.classList.remove('active'));a.classList.add('active')}));
    ['#userSearch','#userPlanFilter','#userBillingFilter'].forEach(s=>$(s)?.addEventListener(s==='#userSearch'?'input':'change',renderUsers));
    ['#feedbackTypeFilter','#feedbackSearch'].forEach(s=>$(s)?.addEventListener(s==='#feedbackSearch'?'input':'change',renderFeedback));
    document.addEventListener('click',e=>{let v=e.target.closest('[data-view-user]');if(v)userDrawer(v.dataset.viewUser);let c=e.target.closest('[data-copy-user]');if(c)copyText(c.dataset.copyUser);let o=e.target.closest('[data-copy-override]');if(o)copyText(JSON.stringify(overrideJSON(o.dataset.copyOverride),null,2));let cl=e.target.closest('[data-clear-override]');if(cl&&confirm('Are you sure? This only clears local override inputs.')){$$(`[data-search-adjust="${CSS.escape(cl.dataset.clearOverride)}"],[data-lead-adjust="${CSS.escape(cl.dataset.clearOverride)}"],[data-note="${CSS.escape(cl.dataset.clearOverride)}"]`).forEach(i=>i.value='');status('Override fields cleared locally','ok')}if(e.target.closest('[data-copy-report]'))copyText('Override JSON copied. Apply it through the configured admin workflow or Sheets update flow.');});
    $('#closeDrawer')?.addEventListener('click',()=>$('#adminDrawer').hidden=true);
  }
  function init(){bind();accessGate();setTimeout(load,350)}
  document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init):init();
})();