(function(){
  'use strict';

  var SHEET_ID='1mFDJKBexMfMn8NZSq7xhES7pHWt4LCEY2Gq-zATHuco';
  var CACHE_KEY='rankforge-user-profile-cache-v1';
  var DEBUG_KEY='rankforge-profile-debug-v1';
  var ADMIN_EMAIL='automly1@gmail.com';
  var MAX_SESSION_WAIT_MS=12000;
  var inFlight=null;
  var lastResolvedAt=0;
  var lastSignature='';
  var REFRESH_TTL_MS=5*60*1000;

  function clean(v){return String(v==null?'':v).trim();}
  function low(v){return clean(v).toLowerCase();}
  function parse(raw,fallback){try{return raw?JSON.parse(raw):fallback;}catch(e){return fallback;}}
  function first(){for(var i=0;i<arguments.length;i++){var v=clean(arguments[i]);if(v)return v;}return '';}
  function isEmail(v){return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(clean(v));}
  function isRealUserId(v){return !!clean(v)&&!isEmail(v);}
  function num(v,fallback){var raw=clean(v);if(/^(infinity|unlimited|∞)$/i.test(raw))return Infinity;var n=Number(raw.replace(/[^0-9.-]/g,''));return Number.isFinite(n)?Math.max(0,Math.round(n)):(fallback||0);}
  function asBool(v){var t=low(v);return v===true||['true','yes','1','enabled','active'].indexOf(t)>=0;}
  function nowIso(){return new Date().toISOString();}
  function signature(p){return [p.email,p.user_id,p.plan,p.billing_status,p.monthly_search_limit,p.monthly_qualified_lead_credit_limit,p.max_leads_per_batch,p.csv_export,p.qualified_leads_used,p.qualified_leads_remaining,p.search_batches_used,p.search_batches_remaining,p.source_row_source,p.source_row_priority,p.source_row_index,p.profile_source].join('|');}

  function readSession(){try{if(window.rankforgeAuth&&typeof window.rankforgeAuth.getSession==='function'){var s=window.rankforgeAuth.getSession();if(s&&(s.email||s.userEmail||s.userId||s.id))return s;}var stored=parse(localStorage.getItem('rankforge-auth-session-v1'),null);if(stored&&(stored.email||stored.userEmail||stored.userId||stored.id))return stored;for(var i=0;i<localStorage.length;i++){var k=localStorage.key(i)||'';if(!/^sb-.+-auth-token$/.test(k))continue;var p=parse(localStorage.getItem(k),{});var u=p.user||(p.currentSession&&p.currentSession.user)||(p.session&&p.session.user);if(u&&(u.email||u.id))return{userId:u.id,id:u.id,email:u.email||'',userEmail:u.email||''};}}catch(e){}return {};}
  function waitForSession(){var started=Date.now();return new Promise(function(resolve){(function tick(){var s=readSession();if(s&&(s.email||s.userEmail||s.userId||s.id))return resolve(s);if(Date.now()-started>=MAX_SESSION_WAIT_MS)return resolve(s||{});setTimeout(tick,250);})();});}

  function normalizePlan(v){var raw=low(v).replace(/\s+/g,'_').replace(/-/g,'_');if(/admin|unlimited/.test(raw))return'admin_unlimited';if(/stack|bundle|combined|multi/.test(raw))return'stacked';if(raw==='growth'||raw==='pro'||raw==='founding'||raw==='founding_plan')return'growth';if(raw==='starter'||raw==='start'||raw==='basic'||raw==='starter_plan')return'starter';return'free';}
  function normalizeBilling(v){var raw=low(v).replace(/\s+/g,'_').replace(/-/g,'_');if(/admin|unlimited/.test(raw))return'admin_unlimited';if(/active|paid|trialing|complete|checkout_complete|subscription_active|subscription/.test(raw))return'active';if(/pending|incomplete/.test(raw))return'pending_payment';if(/cancel/.test(raw))return'canceled';return raw||'free';}
  function defaults(plan){if(plan==='admin_unlimited')return{search:Infinity,credits:Infinity,batch:50,csv:true};if(plan==='stacked')return{search:0,credits:0,batch:50,csv:true};if(plan==='growth')return{search:150,credits:250,batch:50,csv:true};if(plan==='starter')return{search:50,credits:50,batch:25,csv:true};return{search:2,credits:10,batch:10,csv:false};}
  function planLabel(plan){return plan==='admin_unlimited'?'Admin Unlimited':plan==='stacked'?'Stacked Paid':plan==='growth'?'Growth':plan==='starter'?'Starter':'Free';}
  function isPaidPlan(plan,billing){return plan&&plan!=='free'&&(billing==='active'||billing==='admin_unlimited'||billing==='trialing'||billing==='paid');}

  function rowValue(r,k){return r&&Object.prototype.hasOwnProperty.call(r,k)?r[k]:'';}
  function rowSource(r){return low(first(r.source,r.profile_source,r.admin_override,r.override_active));}
  function rowPlan(r){return normalizePlan(first(r.plan_override,rowValue(r,'plan'),rowValue(r,'current_plan'),r.plan_name,r.subscription_plan,r.package,r.tier));}
  function rowBilling(r){return normalizeBilling(first(r.billing_status_override,r.billing_status,r.subscription_status,r.payment_status));}
  function rowEmails(r){var vals=[r.email,r.user_email,r.account_email,r.customer_email,r.billing_email];if(isEmail(r.user_id))vals.push(r.user_id);if(isEmail(r.userId))vals.push(r.userId);return vals.map(low).filter(Boolean);}
  function rowUserId(r){var raw=first(r.user_id,r.userId,r.owner_user_id,r.id);return isRealUserId(raw)?raw:'';}
  function rowTime(r){var t=Date.parse(first(r.updated_at,r.synced_at,r.created_at));return Number.isFinite(t)?t:0;}
  function isAdminOverride(r){var src=rowSource(r);return src==='admin_override'||src==='true'||src==='rankforge_admin_quality_override'||src.indexOf('admin')>=0;}
  function hasPaidSource(r){var src=rowSource(r);return src.indexOf('stripe')>=0||src.indexOf('checkout')>=0||src.indexOf('billing')>=0||src.indexOf('subscription')>=0||src.indexOf('paid')>=0;}
  function isPaidBillingRow(r){var plan=rowPlan(r),billing=rowBilling(r);return plan!=='free'&&(hasPaidSource(r)||billing==='active'||billing==='admin_unlimited'||isAdminOverride(r));}
  function rowPriority(r){if(rowSource(r)==='user_current_state')return 100;if(isAdminOverride(r))return 40;if(isPaidBillingRow(r))return 30;if(rowPlan(r)!=='free')return 15;return 0;}
  function rowTieRank(r){if(rowSource(r)==='user_current_state')return 5;if(isAdminOverride(r))return 3;if(rowSource(r).indexOf('stack')>=0)return 3;if(hasPaidSource(r))return 2;if(rowBilling(r)==='active'&&rowPlan(r)!=='free')return 1;return 0;}

  function parseRows(payload){var cols=(payload.table&&payload.table.cols||[]).map(function(c){return c.label||c.id;});return (payload.table&&payload.table.rows||[]).map(function(row,i){var o={_row_index:i+2};(row.c||[]).forEach(function(cell,idx){o[cols[idx]]=cell?clean(cell.f||cell.v||''):'';});return o;});}
  function fetchSheet(sheetName){return new Promise(function(resolve){var cb='rfSheet_'+sheetName.replace(/\W/g,'_')+'_'+Date.now()+'_'+Math.floor(Math.random()*999999);var script=document.createElement('script');var done=false;var timer=setTimeout(function(){finish([], 'timeout');},12000);function finish(rows,error){if(done)return;done=true;clearTimeout(timer);try{script.remove();}catch(e){}try{delete window[cb];}catch(e){}resolve({rows:rows||[],error:error||'',sheet:sheetName});}window[cb]=function(payload){try{finish(parseRows(payload));}catch(e){finish([],e&&e.message||'parse_error');}};script.src='https://docs.google.com/spreadsheets/d/'+SHEET_ID+'/gviz/tq?sheet='+encodeURIComponent(sheetName)+'&tqx=responseHandler:'+cb+'&headers=1&cacheBust='+Date.now();script.onerror=function(){finish([], 'script_error');};(document.body||document.documentElement).appendChild(script);});}
  function fetchUsersSheet(){return fetchSheet('users');}
  function fetchCurrentStateSheet(){return fetchSheet('user_current_state').then(function(res){res.rows=(res.rows||[]).map(function(r){r.source='user_current_state';return r;});return res;});}
  function matchRows(rows,s){var email=low(s.email||s.userEmail);var uid=clean(s.userId||s.id);return rows.filter(function(r){var emails=rowEmails(r);var realId=rowUserId(r);return (email&&emails.indexOf(email)>=0)||(uid&&realId&&realId===uid);});}
  function chooseBest(rows){return rows.slice().sort(function(a,b){var ap=rowPriority(a),bp=rowPriority(b);if(ap!==bp)return bp-ap;var ar=rowTieRank(a),br=rowTieRank(b);if(ar!==br)return br-ar;var at=rowTime(a),bt=rowTime(b);if(at!==bt)return bt-at;return Number(b._row_index||0)-Number(a._row_index||0);})[0]||null;}
  function stripeIds(row){return{stripe_event_id:first(row.stripe_event_id,row.event_id),stripe_subscription_id:first(row.stripe_subscription_id,row.subscription_id),stripe_invoice_id:first(row.stripe_invoice_id,row.invoice_id),stripe_checkout_session_id:first(row.stripe_checkout_session_id,row.checkout_session_id),stripe_payment_intent_id:first(row.stripe_payment_intent_id,row.payment_intent_id)};}
  function sanitizeAdmin(profile,loginEmail){if(loginEmail!==ADMIN_EMAIL&&profile.plan==='admin_unlimited'){profile.plan='free';profile.current_plan='free';profile.plan_name='Free';profile.billing_status='free';profile.subscription_status='free';profile.monthly_search_limit=2;profile.monthly_qualified_lead_credit_limit=10;profile.max_leads_per_batch=10;profile.csv_export=false;profile.admin_managed=false;profile.paid_managed=false;profile.debug_admin_sanitized=true;}return profile;}
  function paidEntitlementFromProfile(profile){return profile.plan!=='free'&&(profile.admin_managed||profile.paid_managed||profile.csv_export===true||String(profile.csv_export).toLowerCase()==='true'||num(profile.monthly_search_limit,0)>2||num(profile.monthly_qualified_lead_credit_limit,0)>10||/stripe|checkout|billing|subscription|override|paid|user_current_state/i.test(clean(profile.source_row_source||profile.profile_source)));}
  function buildProfile(row,s,meta){
    var loginEmail=low(s.email||s.userEmail||rowEmails(row)[0]);var plan=rowPlan(row);var billing=rowBilling(row);if(loginEmail===ADMIN_EMAIL){plan='admin_unlimited';billing='admin_unlimited';}
    var d=defaults(plan);var searchRaw=first(row.effective_search_limit,row.monthly_search_limit,row.search_batches_limit,row.base_search_limit);var creditRaw=first(row.effective_qualified_lead_limit,row.monthly_qualified_lead_credit_limit,row.qualified_lead_credits_limit,row.base_qualified_lead_limit);var batchRaw=first(row.max_leads_per_batch,row.max_prospects_per_batch,row.max_batch);var csvRaw=first(row.csv_export,row.csv_enabled,row.can_export_csv);var ids=stripeIds(row);
    var profile={user_id:first(s.userId,s.id,rowUserId(row),row.user_id,row.userId),email:first(s.email,s.userEmail,rowEmails(row)[0],row.email,row.user_email),plan:plan,current_plan:plan,plan_name:planLabel(plan),billing_status:billing,subscription_status:billing,monthly_search_limit:searchRaw?num(searchRaw,d.search):d.search,monthly_qualified_lead_credit_limit:creditRaw?num(creditRaw,d.credits):d.credits,max_leads_per_batch:batchRaw?num(batchRaw,d.batch):d.batch,csv_export:csvRaw?asBool(csvRaw):d.csv,qualified_leads_used:num(first(row.qualified_leads_used,row.qualified_leads_consumed),0),qualified_leads_remaining:num(first(row.qualified_leads_remaining,row.remaining_qualified_leads),0),search_batches_used:num(first(row.search_batches_used,row.searches_used),0),search_batches_remaining:num(first(row.search_batches_remaining,row.remaining_search_batches),0),admin_managed:isAdminOverride(row),paid_managed:isPaidBillingRow(row)||rowSource(row)==='user_current_state',profile_source:rowSource(row)==='user_current_state'?'user_current_state':'users_sheet',source_row_source:first(row.source,''),source_row_priority:rowPriority(row),source_row_tie_rank:rowTieRank(row),source_row_index:row._row_index,stack_note:first(row.stack_note,''),stripe_event_id:ids.stripe_event_id,stripe_subscription_id:ids.stripe_subscription_id,stripe_invoice_id:ids.stripe_invoice_id,stripe_checkout_session_id:ids.stripe_checkout_session_id,stripe_payment_intent_id:ids.stripe_payment_intent_id,resolved_at:nowIso(),resolver_version:'user-current-state-v1'};
    if(rowSource(row)==='user_current_state'){profile.debug_using_user_current_state=true;}
    if(plan!=='free'&&billing!=='active'&&billing!=='admin_unlimited'&&paidEntitlementFromProfile(profile)){profile.billing_status='active';profile.subscription_status='active';profile.debug_billing_inferred_active=true;}
    if(meta)profile.debug_meta=meta;return sanitizeAdmin(profile,loginEmail);
  }
  function fallbackProfile(s,reason){var email=low(s.email||s.userEmail);var plan=email===ADMIN_EMAIL?'admin_unlimited':'free';var billing=email===ADMIN_EMAIL?'admin_unlimited':'free';var d=defaults(plan);return {user_id:first(s.userId,s.id),email:first(s.email,s.userEmail),plan:plan,current_plan:plan,plan_name:planLabel(plan),billing_status:billing,subscription_status:billing,monthly_search_limit:d.search,monthly_qualified_lead_credit_limit:d.credits,max_leads_per_batch:d.batch,csv_export:d.csv,qualified_leads_used:0,qualified_leads_remaining:d.credits,search_batches_used:0,search_batches_remaining:d.search,admin_managed:plan==='admin_unlimited',paid_managed:false,profile_source:'default',source_row_source:'',source_row_priority:plan==='admin_unlimited'?40:0,source_row_index:null,resolved_at:nowIso(),resolver_version:'user-current-state-v1',debug_meta:{reason:reason||'no_matching_current_state_or_users_row'}};}
  function cachedMatchesSession(cached,s){var email=low(s.email||s.userEmail);var uid=clean(s.userId||s.id);return cached&&((email&&low(cached.email)===email)||(uid&&clean(cached.user_id)===uid));}
  function cachedIsUsable(cached,s){if(!cachedMatchesSession(cached,s))return false;var email=low(s.email||s.userEmail||cached.email);if(email!==ADMIN_EMAIL&&cached.plan==='admin_unlimited')return false;return isPaidPlan(cached.plan,cached.billing_status)||paidEntitlementFromProfile(cached)||cached.profile_source==='user_current_state'||cached.profile_source==='users_sheet'||cached.profile_source==='force_users_sheet'||cached.profile_source==='checkout_success';}
  function writeProfile(profile,changed){localStorage.setItem(CACHE_KEY,JSON.stringify(profile));localStorage.setItem(DEBUG_KEY,JSON.stringify(profile));localStorage.setItem('rankforge-plan-v1',profile.plan);localStorage.setItem('rankforge-current-plan-v1',profile.plan);localStorage.setItem('rankforge-billing-status-v1',profile.billing_status);localStorage.setItem('rankforge-monthly-search-limit-v1',String(profile.monthly_search_limit));localStorage.setItem('rankforge-monthly-qualified-credit-limit-v1',String(profile.monthly_qualified_lead_credit_limit));localStorage.setItem('rankforge-max-leads-per-batch-v1',String(profile.max_leads_per_batch));localStorage.setItem('rankforge-qualified-leads-used-v1',String(profile.qualified_leads_used||0));localStorage.setItem('rankforge-qualified-leads-remaining-v1',String(profile.qualified_leads_remaining||0));localStorage.setItem('rankforge-search-batches-used-v1',String(profile.search_batches_used||0));localStorage.setItem('rankforge-search-batches-remaining-v1',String(profile.search_batches_remaining||0));localStorage.setItem('rankforge-admin-plan-managed-v1',profile.admin_managed?'true':'false');localStorage.setItem('rankforge-paid-plan-managed-v1',profile.paid_managed?'true':'false');window.rankforgeUserProfile=profile;if(changed)window.dispatchEvent(new CustomEvent('rankforge:user-profile-resolved',{detail:profile}));}
  function setText(id,value){var el=document.getElementById(id);if(el)el.textContent=value;}
  function fmtLimit(v,suffix){return v===Infinity?'Unlimited':String(v)+(suffix||'');}
  function applyDom(profile,changed){if(!changed)return;var name=planLabel(profile.plan);setText('rfPlanBadge',name+' Plan');setText('rfProspectPlanBadge',name+' Plan');setText('rfSearchPlanBadge',name+' Plan');var remaining=profile.monthly_qualified_lead_credit_limit===Infinity?'Unlimited qualified lead credits':(profile.qualified_leads_remaining!==undefined&&profile.profile_source==='user_current_state'?profile.qualified_leads_remaining:profile.monthly_qualified_lead_credit_limit)+' credits remaining';setText('rfCreditBadge',remaining);setText('rfProspectCreditBadge',remaining);setText('rfSearchUsageBadge',profile.monthly_search_limit===Infinity?'Unlimited batches':profile.monthly_search_limit+' batches');var card=document.getElementById('rfMvpUsageCard');if(card){var title=card.querySelector('h2');if(title)title.textContent=name+' Plan';var pill=card.querySelector('.rf-mvp-pill');if(pill)pill.textContent=remaining;}setText('accountPlanName',name);setText('accountPlanStatus',profile.billing_status==='active'||profile.billing_status==='admin_unlimited'?'Active':profile.billing_status);setText('accountLeadLimit',fmtLimit(profile.monthly_qualified_lead_credit_limit,' qualified leads/month'));setText('accountSearchLimit',fmtLimit(profile.monthly_search_limit,' search batches/month')+' · max '+fmtLimit(profile.max_leads_per_batch,' leads/batch'));setText('accountWebhookStatus',profile.csv_export?'Enabled':'Plan gated');setText('accountWebhookValue',profile.csv_export?'CSV export enabled for this plan.':'CSV export requires Starter, Growth, or admin.');}
  async function resolveEffectiveUserProfile(options){options=options||{};var cached=parse(localStorage.getItem(CACHE_KEY),null);if(!options.force&&cached&&Date.now()-lastResolvedAt<REFRESH_TTL_MS){window.rankforgeUserProfile=cached;return cached;}if(inFlight)return inFlight;inFlight=(async function(){var session=options.session||await waitForSession();if(!session||(!session.email&&!session.userEmail&&!session.userId&&!session.id)){var noSession=fallbackProfile({},'no_session_email_or_user_id');localStorage.setItem(DEBUG_KEY,JSON.stringify(noSession));return noSession;}var loginEmail=low(session.email||session.userEmail);if(cachedIsUsable(cached,session)){cached=sanitizeAdmin(cached,loginEmail);if(cached.plan!=='free'&&cached.billing_status!=='active'&&paidEntitlementFromProfile(cached)){cached.billing_status='active';cached.subscription_status='active';cached.debug_billing_inferred_active=true;}window.rankforgeUserProfile=cached;writeProfile(cached,signature(cached)!==lastSignature);lastSignature=signature(cached);}
      var current=await fetchCurrentStateSheet();var currentMatches=matchRows(current.rows,session);var selected=chooseBest(currentMatches);var sourceResult=current;var usersResult=null;var usersMatches=[];
      if(!selected){usersResult=await fetchUsersSheet();usersMatches=matchRows(usersResult.rows,session);selected=chooseBest(usersMatches);sourceResult=usersResult;}
      var profile;if(selected){profile=buildProfile(selected,session,{current_state_matched_rows:currentMatches.length,users_matched_rows:usersMatches.length,current_state_total_rows:current.rows.length,users_total_rows:usersResult?usersResult.rows.length:0,current_state_error:current.error||'',users_error:usersResult&&usersResult.error||'',selected_sheet:sourceResult.sheet});}else if(cachedIsUsable(cached,session)){profile=cached;profile.debug_meta=Object.assign({},profile.debug_meta||{},{sheet_miss_preserved_cached_profile:true,current_state_error:current.error||'',current_state_matched_rows:currentMatches.length,current_state_total_rows:current.rows.length});profile.profile_source=profile.profile_source||'cached_preserved';profile.resolved_at=nowIso();}else{profile=fallbackProfile(session,current.error?'user_current_state_fetch_'+current.error:'no_exact_match');}
      profile=sanitizeAdmin(profile,loginEmail);var sig=signature(profile);var changed=sig!==lastSignature;lastSignature=sig;lastResolvedAt=Date.now();writeProfile(profile,changed);applyDom(profile,changed);return profile;})().finally(function(){inFlight=null;});return inFlight;}
  window.rankforgeResolveEffectiveUserProfile=resolveEffectiveUserProfile;window.rankforgeResolveUserPlan=function(){return resolveEffectiveUserProfile({force:true});};
  function start(){resolveEffectiveUserProfile({force:true,reason:'initial_load'});}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();
  window.addEventListener('focus',function(){resolveEffectiveUserProfile({force:true,reason:'focus'});});
  window.addEventListener('storage',function(e){if(e.key==='rankforge-auth-session-v1')resolveEffectiveUserProfile({force:true,reason:'auth_storage'});});
})();