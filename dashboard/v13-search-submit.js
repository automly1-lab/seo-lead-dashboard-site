(function(){
  'use strict';
  if (typeof state === 'undefined') return;

  var CFG=window.RANKFORGE_CONFIG||{WEBHOOKS:{}};
  var BASE=String(CFG.N8N_BASE_URL||'https://rankforge1907.app.n8n.cloud').replace(/\/$/,'');
  var SEARCH_WEBHOOK=BASE+(CFG.WEBHOOKS&&CFG.WEBHOOKS.CREATE_SEARCH||'/webhook/rankforge-demo-create-search');
  var CURRENT_STATE=BASE+(CFG.WEBHOOKS&&CFG.WEBHOOKS.CURRENT_STATE||'/webhook/rankforge-demo-current-state');
  var CREDIT_RULE=CFG.CREDIT_RULE||'Credits are consumed ONLY when a lead is marked as Qualified.';
  var ADMIN_EMAIL=String(CFG.ADMIN_EMAIL||'automly1@gmail.com').toLowerCase();

  function clean(v){return String(v==null?'':v).trim()}
  function n(v,d){var x=Number(clean(v).replace(/[^0-9.-]/g,''));return Number.isFinite(x)?Math.max(0,Math.round(x)):(d||0)}
  function byId(id){return document.getElementById(id)}
  function val(id){return clean(byId(id)&&byId(id).value)}
  function key(v){var r=clean(v).toLowerCase().replace(/\s+/g,'_').replace(/-/g,'_');if(['pro','founding','founding_plan'].indexOf(r)!==-1)return'growth';if(['start','basic','starter_plan'].indexOf(r)!==-1)return'starter';if(['agency','enterprise'].indexOf(r)!==-1)return'agency_intelligence';if(r.indexOf('admin')!==-1)return'admin_unlimited';return r||'free'}
  function defaults(k){return{free:{b:1,c:10,m:10,csv:false},starter:{b:50,c:50,m:25,csv:true},growth:{b:150,c:250,m:50,csv:true},agency_intelligence:{b:9999,c:999999,m:50,csv:true},admin_unlimited:{b:999999,c:999999,m:50,csv:true,u:true}}[key(k)]||{b:1,c:10,m:10,csv:false}}
  function session(){if(window.rankforgeAuth&&typeof window.rankforgeAuth.getSession==='function')return window.rankforgeAuth.getSession();try{return JSON.parse(localStorage.getItem('rankforge-auth-session-v1')||'null')}catch(e){return null}}
  function identity(){var s=session()||{};return{userId:clean(s.userId||s.id||s.user_id),email:clean(s.email||s.user_email).toLowerCase()}}
  function isAdmin(){return identity().email===ADMIN_EMAIL}
  function cachedPlan(){try{return JSON.parse(localStorage.getItem('rankforge-user-plan-v1')||'null')}catch(e){return null}}
  function rowMatches(row){var id=identity(),uid=clean(row&&(row.user_id||row.owner_user_id||row.created_by)),em=clean(row&&(row.email||row.user_email||row.owner_email||row.email_user)).toLowerCase();if(uid||em)return(!!uid&&!!id.userId&&uid===id.userId)||(!!em&&!!id.email&&em===id.email);return true}
  function normalizePlan(row){var k=key(row&&(row.key||row.plan||row.current_plan||row.name)||state.plan&&(state.plan.key||state.plan.name)||localStorage.getItem('rankforge-selected-plan-v1')),d=defaults(k);return{key:k,name:k,billingStatus:clean(row&&(row.billing_status||row.billingStatus||row.subscription_status))||(k==='free'?'free':'active'),batchesLimit:n(row&&(row.effective_search_limit||row.search_batches_limit||row.search_batch_limit||row.batchesLimit),d.b),batchesUsed:n(row&&(row.search_batches_used||row.batchesUsed),0),creditsLimit:n(row&&(row.effective_qualified_lead_limit||row.qualified_lead_credits_limit||row.qualified_leads_limit||row.creditsLimit),d.c),creditsUsed:n(row&&(row.qualified_lead_credits_used||row.qualified_leads_used||row.creditsUsed),0),maxLeadsPerBatch:n(row&&(row.max_leads_per_batch||row.maxLeadsPerBatch),d.m),csvExport:!!(row&&(row.csvExport||/^(true|yes|1|enabled)$/i.test(clean(row.csv_export))))||d.csv,unlimited:!!(row&&(row.unlimited||/^(true|yes|1)$/i.test(clean(row.admin_unlimited))))||d.u||isAdmin()}}
  function savePlan(p){state.plan=Object.assign({},state.plan||{},p);window.state=state;localStorage.setItem('rankforge-user-plan-v1',JSON.stringify(p));localStorage.setItem('rankforge-selected-plan-v1',p.key);localStorage.setItem('rankforge-billing-status-v1',p.billingStatus||'')}
  async function freshPlan(){var s=session()||{},fallback=normalizePlan(cachedPlan()||state.plan||{});if(!s.email&&!s.userId&&!s.id)return fallback;try{var body=new URLSearchParams();body.set('email',s.email||'');body.set('user_id',s.userId||s.id||'');body.set('event','get_current_state_for_search');var res=await fetch(CURRENT_STATE,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded;charset=UTF-8','Accept':'application/json'},body:body.toString()});if(!res.ok)throw new Error('state '+res.status);var json=await res.json();var row=json.current_state||json.user||json.row||json.data||json;if(Array.isArray(row))row=row[0]||{};if(!rowMatches(row))throw new Error('current_state_identity_mismatch');var p=normalizePlan(row);savePlan(p);return p}catch(e){console.warn('Plan lookup failed before search, cached plan used',e);savePlan(fallback);return fallback}}
  function scopedSearchKey(payload){return 'rankforge:'+((payload.user_id||payload.user_email||payload.email||'anonymous'))+':searches'}
  function legacyStorageKey(payload){return 'rankforge-dashboard-data-v2::'+(payload.user_id||payload.user_email||payload.email||'anonymous')}
  function searchObj(payload,status){return{id:payload.search_id,search_id:payload.search_id,user_id:payload.user_id,email:payload.email||payload.user_email,demo_sandbox:true,source_environment:'demo',name:payload.search_name,niche:payload.niche,city:payload.city,country:payload.country,keyword:payload.primary_keyword,include:payload.include_terms,exclude:payload.exclude_terms,depth:payload.max_results_requested,mode:payload.qualification_mode,businessType:payload.business_type,seoThreshold:payload.min_audit_score,leadThreshold:payload.min_lead_score,status:status||'Pending',raw:0,audited:0,qualified:0,review:0,rejected:0,contacts:0,created:payload.created_at,updated_at:new Date().toISOString()}}
  function upsertSearch(payload,status){var batch=searchObj(payload,status);state.searches=[batch].concat((state.searches||[]).filter(function(x){return(x.search_id||x.id)!==payload.search_id}));try{var k=scopedSearchKey(payload),data=JSON.parse(localStorage.getItem(k)||'[]');data=[batch].concat((Array.isArray(data)?data:[]).filter(function(x){return(x.search_id||x.id)!==payload.search_id}));localStorage.setItem(k,JSON.stringify(data))}catch(e){}try{var lk=legacyStorageKey(payload),legacy=JSON.parse(localStorage.getItem(lk)||'{"searches":[],"leads":[]}');legacy.searches=[batch].concat((legacy.searches||[]).filter(function(x){return(x.search_id||x.id)!==payload.search_id}));legacy.updated_at=new Date().toISOString();localStorage.setItem(lk,JSON.stringify(legacy))}catch(e){}if(window.rankforgeDashboardData&&typeof window.rankforgeDashboardData.saveSearch==='function')window.rankforgeDashboardData.saveSearch(batch);document.dispatchEvent(new CustomEvent('rankforge:rendered',{detail:{reason:'search_'+(status||'pending'),search_id:payload.search_id}}));return batch}
  function payload(plan){var s=session()||{},email=s.email||state.user&&state.user.email||'',id='srch_demo_'+Date.now(),depth=val('searchDepthInput')||String(plan.maxLeadsPerBatch||25),max=depth.replace(/[^0-9]/g,'')||String(plan.maxLeadsPerBatch||25);return{search_id:id,id:id,user_id:s.userId||s.id||state.user&&state.user.userId||'',email:email,user_email:email,owner_email:email,admin_unlimited:plan.unlimited?'true':'false',demo_sandbox:'true',source_environment:'demo',created_at:new Date().toISOString(),updated_at:new Date().toISOString(),status:'active',search_name:val('searchNameInput'),niche:val('nicheInput'),business_type:val('businessTypeInput')||'local service business',city:val('cityInput'),country:val('countryInput'),primary_keyword:val('keywordSeedInput')||val('nicheInput'),secondary_keywords:val('includeTermsInput'),include_terms:val('includeTermsInput'),exclude_terms:val('excludeTermsInput'),search_depth:depth,discovery_query_limit:plan.key==='free'?'1':'4',discovery_page_limit:plan.key==='free'?'1':'2',max_results_requested:max,contact_requirement:'either',exclude_chains_franchises:'true',qualification_mode:val('qualificationModeInput')||'Strict evidence',min_audit_score:val('seoThresholdInput')||'60',min_lead_score:val('leadThresholdInput')||'70',plan:plan.key,current_plan:plan.key,billing_status:plan.billingStatus||'active',search_batch_limit:String(plan.batchesLimit),search_batches_limit:String(plan.batchesLimit),search_batches_used:String(plan.batchesUsed||0),effective_search_limit:String(plan.batchesLimit),qualified_lead_credits_limit:String(plan.creditsLimit),qualified_leads_limit:String(plan.creditsLimit),qualified_lead_credits_used:String(plan.creditsUsed||0),qualified_leads_used:String(plan.creditsUsed||0),effective_qualified_lead_limit:String(plan.creditsLimit),max_leads_per_batch:String(plan.maxLeadsPerBatch||25),csv_export:plan.csvExport?'true':'false',credit_note:'demo_sandbox=true; source_environment=demo; search_id='+id,details_json:JSON.stringify({demo_sandbox:true,source_environment:'demo',search_id:id}),plan_source:'dashboard_demo_current_state_endpoint'}}
  async function send(payload){var body=new URLSearchParams();Object.keys(payload).forEach(function(k){body.set(k,payload[k]==null?'':String(payload[k]))});var res=await fetch(SEARCH_WEBHOOK,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded;charset=UTF-8','Accept':'application/json'},body:body.toString()});if(!res.ok)throw new Error('create_search_'+res.status);return res}
  function setStatus(msg,tone){var node=byId('createSearchStatus');if(!node)return;node.textContent=msg||'';node.style.display=msg?'':'none';node.classList.remove('is-error','is-success','is-loading');if(tone)node.classList.add('is-'+tone)}
  function injectRule(){var summary=byId('createSummary');if(summary&&!summary.querySelector('.rf-credit-rule'))summary.insertAdjacentHTML('beforeend','<span class="rf-credit-rule">'+CREDIT_RULE+'</span>')}
  async function submit(e){e.preventDefault();e.stopPropagation();if(e.stopImmediatePropagation)e.stopImmediatePropagation();setStatus('','');var btn=byId('createBatch');if(btn){btn.disabled=true;btn.textContent='Starting...'}try{var p=await freshPlan();if(!p.unlimited&&n(p.batchesLimit,0)<=n(p.batchesUsed,0)){setStatus('Your search batch allowance is used up for this plan.','error');return}if(!p.unlimited&&n(p.creditsLimit,0)<=n(p.creditsUsed,0)){setStatus('Your qualified lead credits are used up for this plan.','error');return}var pl=payload(p);if(!pl.user_id&&!pl.email){setStatus('Please sign in before creating a private search batch.','error');return}if(!pl.search_name||!pl.niche||!pl.city||!pl.country||!pl.primary_keyword){setStatus('Please fill Market name, Niche, City, Country, and Primary keyword.','error');return}upsertSearch(pl,'Pending');if(typeof render==='function')render();await send(pl);upsertSearch(pl,'Running');state.plan.batchesUsed=Number(state.plan.batchesUsed||0)+1;if(typeof render==='function')render();var modal=byId('modal');if(modal)modal.classList.remove('open');document.querySelector('[data-view="searches"]')&&document.querySelector('[data-view="searches"]').click();setTimeout(function(){if(window.rankforgeDashboardData&&window.rankforgeDashboardData.syncResults)window.rankforgeDashboardData.syncResults({force:true,reason:'new-search'})},6500)}catch(err){console.error(err);setStatus('Search could not be started. Please try again.','error')}finally{if(btn){btn.disabled=false;btn.textContent='Start Search Batch'}}}
  function bind(){injectRule();var b=byId('createBatch');if(b&&!b.dataset.v13Bound){b.dataset.v13Bound='true';b.addEventListener('click',submit,true)}document.body.addEventListener('click',function(e){if(e.target&&e.target.id==='createBatch')submit(e)},true)}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind);else bind();
})();